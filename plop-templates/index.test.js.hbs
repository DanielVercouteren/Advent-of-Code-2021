const { challengeOne, challengeTwo } = require('./index')

const input = ``

describe('Challenges for day {{day}}', () => {
    test('Should give the output of challenge 1, day {{day}} with test input', () => {
        expect(challengeOne(input)).toBe(null)
    })

    test('Should give the output of challenge 2, day {{day}} with test input', () => {
        expect(challengeTwo(input)).toBe(null)
    })
})